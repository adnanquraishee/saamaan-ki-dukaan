# Only required variable. Without it the app runs fully; LLM steps fall back to deterministic templates.
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
# Optional Gemini model override
# GEMINI_MODEL=gemini-2.5-flash
# Optional override (default claude-opus-5)
# ANTHROPIC_MODEL=claude-opus-5
