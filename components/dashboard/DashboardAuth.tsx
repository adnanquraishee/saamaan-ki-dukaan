"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
const AUTH_KEY = "anvaya:dashboard-auth";
export function isDashboardAuthenticated() { return typeof window !== "undefined" && sessionStorage.getItem(AUTH_KEY) === "ok"; }
export function DashboardAuth() { const router = useRouter(); const [password, setPassword] = useState(""); const [error, setError] = useState(""); useEffect(() => { if (isDashboardAuthenticated()) router.replace("/dashboard"); }, [router]); function submit(e: React.FormEvent) { e.preventDefault(); if (password === "admin@123") { sessionStorage.setItem(AUTH_KEY, "ok"); router.replace("/dashboard"); } else setError("Incorrect admin password."); } return <main className="tower grid min-h-screen place-items-center bg-tower-bg p-6 text-tower-text"><form onSubmit={submit} className="w-full max-w-sm rounded-sm border border-tower-line bg-tower-panel p-6 shadow-2xl"><p className="font-mono text-[11px] uppercase tracking-[0.22em] text-tower-cyan">Saamaan ki Dukaan · control tower</p><h1 className="mt-2 text-2xl font-semibold text-white">Admin sign in</h1><p className="mt-2 text-sm text-tower-dim">Enter the demo administrator password to view live agent operations.</p><label className="mt-6 block font-mono text-xs text-tower-dim">Password<input autoFocus type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="mt-2 w-full rounded-sm border border-tower-line bg-[#0b1219] px-3 py-2 text-white outline-none focus:border-tower-cyan" /></label>{error && <p className="mt-2 font-mono text-xs text-tower-red">{error}</p>}<button className="mt-5 w-full rounded-sm bg-tower-cyan px-4 py-2 font-mono text-xs font-semibold uppercase tracking-widest text-black hover:bg-teal-300">Enter dashboard</button></form></main>; }
